const fs = require("fs"); console.log("Creating export..."); const data = JSON.parse(fs.readFileSync("./scraped-data-with-images.json", "utf8")); console.log("Loaded", data.pages.length, "pages"); let html = "<!DOCTYPE html><html><head><meta charset=\"UTF-8\"><title>Flex HRM</title></head><body><h1>Flex HRM Dokumentation</h1>"; html += "<p>Totalt: " + data.pages.length + " sidor</p>"; for (let i = 0; i < 5; i++) { const page = data.pages[i]; html += "<h2>" + page.title + "</h2>"; html += "<p>" + page.content.substring(0, 200) + "...</p>"; } html += "</body></html>"; fs.writeFileSync("flex-hrm-preview.html", html); console.log("Preview created: flex-hrm-preview.html");
